#include<stdio.h>
int fact(int a);
int main(){
    printf(" the factorial is %d",fact(9));
    return 0;
}
int fact(int a)
 {
    int product=1;
    for (int i = 1; i<=a; i++)
    {
        product=product*i;
    }
    return product;
 }
    
